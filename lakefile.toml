name = "lean-autosubst"
version = "0.1.0"
defaultTargets = ["Autosubst"]

[[lean_lib]]
name = "Autosubst"

# The reference-signature test suite (see tests/Tests.lean). Build with `lake build Tests`.
[[lean_lib]]
name = "Tests"
srcDir = "tests"
globs = ["Tests", "Tests.+"]
